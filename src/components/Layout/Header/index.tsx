import Logo from "../Logo";
import { StyledHeader } from "./StyledHeader";

function Header() {
  return (
    <>
      <StyledHeader>
        <Logo />
      </StyledHeader>
    </>
  );
}

export default Header;
